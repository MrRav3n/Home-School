import { Subject } from './Subject';

export class Classrom {
  className: string;
  creatorID: string;
  id: string;
  members: [string];
  membersAmount: number;
  schoolName: string;
  subjects: [Subject];
}
